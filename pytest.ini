[pytest]
# Every test module imports `backend.app.…`, so the repo root has to be on
# `sys.path`. Until now that worked only when pytest happened to be started from
# the repo root; run it from here, as this project's README says to, and every
# module failed to import.
pythonpath = ..
testpaths = tests
